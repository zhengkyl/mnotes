
// files is a JSON-like object describing the file structure
// basically exactly how it is on the NoSQL database
// exactly what you think when you imagine a folder/file structure

// onSelectFile is a function that takes the file 
// (probably described as the path to allow duplicate filenames)
// and loads the file contents into the editor
function FileExploer({files, onSelectFile}) {



  return (
    <div></div>
  )
}

export default FileExploer